export type ClubType = {
    id: number,
    title: string
    description: string
    foundationDate: Date
    location: string
    showMembers: true
    presidentId: number
    membersIds: number[]
    createdAt: string
    updatedAt: string
    profilePicture: string
}